{
  "compilerOptions": {
    // 生成js的运行环境
    "target": "esnext",
    // 解析非相对模块名的基准目录
    "baseUrl": "./",
    // 生成模块的类别
    "module": "esnext",
    // 严格检查
    "strict": true,
    "jsx": "preserve",
    // 是否生成sourceMap
    "sourceMap": false,
    // 是否解析json文件
    "resolveJsonModule": true,
    // 指定模块解析策略
    "moduleResolution": "Node",
    // lib.d.ts公共库
    "lib": [
      "esnext",
      "dom"
    ],
    // 解析路径
    "paths": {
      "@/*": [
        "src/*"
      ],
    },
  },
  // 解析文件
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.tsx",
    "src/**/*.vue"
  ],
  // ts 排除的文件
  "exclude": [
    "node_modules"
  ]
}